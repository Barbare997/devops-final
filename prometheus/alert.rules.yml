groups:
  - name: app_alerts
    rules:
      - alert: HighApplicationErrorRate
        expr: sum(increase(app_errors_total[1m])) > 5
        for: 0m
        labels:
          severity: critical
        annotations:
          summary: Application error rate exceeds 5 per minute
          description: More than 5 HTTP error responses occurred in the last minute.
