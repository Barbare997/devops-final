global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alert.rules.yml

scrape_configs:
  - job_name: todo-app
    static_configs:
      - targets: ['app:3000']
    metrics_path: /metrics
